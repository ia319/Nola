import { SettingsPlaceholder } from './SettingsPlaceholder'

export function SystemInfoTab() {
  return (
    <SettingsPlaceholder
      title="System Info"
      description="Prepare the system maintenance surface before connecting the service checks."
    />
  )
}
