import { SettingsPlaceholder } from './SettingsPlaceholder'

export function ExportTab() {
  return (
    <SettingsPlaceholder
      title="Export"
      description="Prepare the export defaults surface before wiring the final settings controls."
    />
  )
}
