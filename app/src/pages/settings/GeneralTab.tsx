import { SettingsPlaceholder } from './SettingsPlaceholder'

export function GeneralTab() {
  return (
    <SettingsPlaceholder
      title="General"
      description="Prepare the general settings surface before wiring the final form controls."
    />
  )
}
