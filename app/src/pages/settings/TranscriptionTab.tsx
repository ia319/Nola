import { SettingsPlaceholder } from './SettingsPlaceholder'

export function TranscriptionTab() {
  return (
    <SettingsPlaceholder
      title="Transcription"
      description="Prepare the transcription defaults surface before connecting the config forms."
    />
  )
}
