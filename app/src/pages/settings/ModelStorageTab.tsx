import { SettingsPlaceholder } from './SettingsPlaceholder'

export function ModelStorageTab() {
  return (
    <SettingsPlaceholder
      title="Model Storage"
      description="Prepare the model storage surface before connecting the settings API."
    />
  )
}
