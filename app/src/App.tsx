import { TaskWorkbenchPage } from '@/pages/task-workbench/TaskWorkbenchPage'

/**
 * @deprecated Import TaskWorkbenchPage from src/pages/task-workbench instead.
 * Keep this wrapper while the shell migration moves routes onto page components.
 */
function App() {
  return <TaskWorkbenchPage />
}

export default App
