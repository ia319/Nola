{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allows the main desktop window to use the core Tauri runtime.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
